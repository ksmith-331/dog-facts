// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.dog.ceo',
      'cdn2.thedogapi.com',
    ],
  },
};

module.exports = nextConfig;
